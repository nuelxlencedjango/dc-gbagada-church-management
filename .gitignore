# Environment secrets — never commit these
.env
.env.*
!.env.example

# Python
venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# Node
node_modules/
build/
dist/

# Uploads (user-generated content, not code)
gbagada_backend/uploads/

# Celery
celerybeat-schedule
celerybeat.pid

# OS/editor cruft
.DS_Store
.vscode/
.idea/

# Database backups — may contain real member PII, never commit
*.sql
*_backup_*.sql

# Celery runtime state (actual generated filename has .db extension)
celerybeat-schedule.db
celerybeat-schedule
celerybeat.pid
